**/node_modules
**/.git
**/.idea
**/.vscode
**/dist
**/build
**/tmp
# Keep secrets out of the build context — .env is injected at runtime via
# docker-compose env_file, never baked into an image layer.
.env
